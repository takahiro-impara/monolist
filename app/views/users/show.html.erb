<!--discriptionをセット-->
<% content_for :head do %>
  <meta name=”description” content="<%= @user.name %>がチェックしているモノリストです。">
<% end %>

<%= render 'profile' , user: @user %>

<%= render 'items/list' , items: @items %>